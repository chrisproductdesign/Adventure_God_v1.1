[
  {
    "name": "createGameObject",
    "description": "Create a GameObject in the currently-open scene",
    "input_schema": {"type":"object","properties":{"name":{"type":"string"}},"required":["name"]},
    "output_schema": {"type":"object","properties":{"success":{"type":"boolean"}}}
  },
  {
    "name": "runPlayMode",
    "description": "Start or stop the editor playmode. input {action:'start'|'stop'}",
    "input_schema": {"type":"object","properties":{"action":{"type":"string"}},"required":["action"]},
    "output_schema": {"type":"object","properties":{"isPlaying":{"type":"boolean"}}}
  },
  {
    "name": "takeScreenshot",
    "description": "Capture a screenshot inside the editor: input {path:'Assets/.../shot.png'}",
    "input_schema": {"type":"object","properties":{"path":{"type":"string"}},"required":["path"]},
    "output_schema": {"type":"object","properties":{"success":{"type":"boolean"}}}
  },
  {
    "name": "buildProject",
    "description": "Build the Unity project (prefer Unity CLI for headless builds)",
    "input_schema": {"type":"object","properties":{},"required":[]},
    "output_schema": {"type":"object","properties":{"success":{"type":"boolean"}}}
  }
]
